import React from 'react';

// Create the App component. This is the root component where all other components reside
// For now a functional component
const App = () => {
  return <div>Hi There!</div>;
};

export default App; //We import this into the index js file. see index js file render method